// Get quick summary statistics from some tabular data
// - on triggers a group by operation
// - metrics are an enum of "sum", "mean", "median", "mode", "min", "max", "range", "count", "variance", "stddev" and "describe"
